# Monorepo root — applies to whole repo (subfolder .gitignore files still apply below)

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
*.iml
.vscode/
*.code-workspace

# Python (e.g. frontend/.venv for write_zh_hk.py)
.venv/
venv/
__pycache__/
*.pyc

# =============================================================================
# Secrets / local config — NEVER commit
# Templates (*.template, env.*.example) stay tracked.
# =============================================================================

# Dotenv (all packages)
.env
.env.*
**/.env
**/.env.*
!**/.env.*.example
!**/env.*.example

# Deploy local secrets. Tracked template is deploy/env.prod.example (no leading dot).
deploy/.env.prod
deploy/.env.prod.*
!deploy/.env.prod.*.example

# Spring Boot local overrides (real DB / Stripe / JWT / MinIO)
**/application.properties
**/application-dev.properties
**/application-local.properties
**/application-secret.properties
# Keep safe committed stubs + templates:
#   application-prod.properties, application-staging.properties
#   application*.properties.template

# Keys, certs, credential dumps
*.pem
*.p12
*.jks
*.keystore
id_rsa
id_rsa.pub
*.secret
**/credentials.json
**/service-account*.json
**/google-services.json

# Node (frontend)
**/node_modules/
**/.angular/cache
**/dist/

# Java (backend)
**/target/

# Logs & local tooling
*.log
.npm/

# Private notes / study guides — keep local, not on the public repo
guides/
notes/
DOCKER.md
USEFUL_LINKS.md
CATEGORY_GUIDE.md
product_guide.md
product_detail_guide.md
DEPLOYMENT_PLAN.md
deploy/Caddyfile
deploy/GIT_MONOREPO_FIX.md
deploy/HETZNER_SERVER_SETUP.md
