# Storefront (`my-website`)

Angular 21 storefront with SSR. Root project README: [../../README.md](../../README.md)

```bash
npm install
npm start
```

Dev server: http://localhost:4200 — expects the API at http://localhost:8080.
